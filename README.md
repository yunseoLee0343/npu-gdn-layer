# npu-gdn-layer
A high-performance NPU runtime middle-layer for Qwen3-Next GDN stateful paths. Bridges the gap between Intel NPU (OpenVINO/ivpu) memory policies and LLM state transitions with zero-copy synchronization and lock-free submission ordering.
